package com.docspot.eval;

/**
 * Evaluate Mathematical Expressions
 *
 */
public class Eval {
	public static void main(String[] args) {
		String s = "5+3";

		for (int i = 0; i < s.length(); i++) {
			char c = s.charAt(i);

		}
	}

	private Double parse(char c) {
		Double x = 0.0;
		if (c == '+') {
		} else if (c == '-') {
		} else if (c == '*') {
		} else if (c == '/') {
		}
		return x;
	}
}
